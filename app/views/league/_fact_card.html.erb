<%# locals: (kicker:, value:, note:) %>
<div class="blueprint flex flex-col gap-0.5 px-4.5 py-4">
  <%= blueprint_corners %>
  <span class="card-kicker"><%= kicker %></span>
  <span class="font-heading text-[38px] font-semibold leading-none"><%= value %></span>
  <span class="text-xs text-ink/55"><%= note %></span>
</div>

<%# locals: (kicker:, value:, who:, detail:) %>
<div class="blueprint flex flex-col gap-1.5 p-4.5">
  <%= blueprint_corners %>
  <span class="card-kicker"><%= kicker %></span>
  <span class="font-heading text-[46px] font-semibold leading-none"><%= value %></span>
  <span class="text-sm font-medium"><%= who %></span>
  <span class="text-xs text-ink/55"><%= detail %></span>
</div>
